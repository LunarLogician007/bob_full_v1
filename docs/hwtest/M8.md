# M8 hardware test: yosys-synthesised designs on the M7 fabric

No RTL change and **no Vivado rebuild**: the M7 bitstream (`bob_top`, IDCODE `0x9BEEF093`) stays in the PL. `hw/build.cfg` stays tagged M7; `hwtest` prints a note about that.

What M8 adds is the guest flow up to placement, for every design in `examples/`:
1. Synthesis: `tools/bob/synth.py` runs yosys on bob's cell library (`$lut`, `BOB_FDRE`/`BOB_FDSE`, `BOB_ADD`, `BOB_BRAM18`, `BOB_DSP`).
2. Equivalence: `tools/bob/equiv.py` checks the netlist against the source in iverilog.
3. Placement: `tools/bob/place.py` packs, places and routes on the fabric.
4. Board comparison: `host/hwtest.py` compares the board with the source Verilog.

## Before

- M7 has passed (`make hwtest M=M7`) and its bitstream is programmed.
- `make check` is green on the Mac, which includes `sim/run_synth_sim.sh`: the same placed designs on the complete FPGA RTL.

## Automatic (`make hwtest M=M8`)

The regression runs first: `idcode` (`0x9BEEF093`), `bypass`, `selftest`, then `chain-length` (9400).

Each `synth-*` check:
1. rebuilds the design from its Verilog
2. loads the chain
3. writes any BRAM contents over USER4 before startup
4. runs JSTART
5. clocks the fabric once per INTEST scan (USER1 autostep), for 64 clocks
6. requires the LEDs after every clock to equal the source Verilog's output under the same inputs

| check | example | cells |
|---|---|---|
| synth-gates | `examples/gates.v`: AND, 6-input parity, mux | 3 `$lut` |
| synth-adder | `examples/adder.v`: `sw + btn[1:0]` | 3 `BOB_ADD` (carry chain) |
| synth-counter | `examples/counter.v`: 6-bit, enable BTN0, sync reset BTN1 | 6 `BOB_ADD` + 6 `BOB_FDRE` (CE/R on the FF pins) |
| synth-blinky | `examples/blinky.v`: 12-bit free counter | 12 `BOB_ADD` + 12 `BOB_FDRE`; the chain crosses two columns |
| synth-ram | `examples/ram.v`: inferred block RAM | 1 `BOB_BRAM18`, initial contents over USER4 |
| synth-mult | `examples/mult.v`: registered 8 × 6 multiplier | 1 `BOB_DSP` + 13 `BOB_FDRE` + 3 `$lut` |

## By hand (`--manual`)

- [ ] `cd host && ./fpga.py --watch` after `make hwtest M=M8`: LD3 on, the board still responds (the last design is left loaded)
