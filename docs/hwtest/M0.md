# M0 hardware test: copy + `hw/` layout + adaptable Tcl

No RTL logic changed in M0. The test proves that the new folder and script flow builds the same working fabric and that the project is reused.

## Automatic (`make hwtest M=M0`)

| check | pass means |
|---|---|
| idcode | reads `0x3BEEF093`, the `build.cfg` value passed in as a generic |
| bypass | BYPASS measures exactly 1 bit |
| selftest | all `designs.py` designs load, read back, and match the model through INTEST |
| showcase-live | `showcase` loaded; real LEDs equal the model for the current switch positions |

## By hand (`--manual` asks these)

- [ ] First run of build.tcl printed "creating project" and ended with "build OK"
- [ ] Edited one comment in hw/src/clb/tile.v on the Mac, pasted hw again, re-ran: it printed "reusing existing project" and "inputs changed", and did not create a new project
- [ ] Pasted hw again with no edits and re-ran: it printed "skipping synthesis and implementation"
- [ ] With showcase loaded, SW0/SW1 give AND on LD0, OR on LD1, XOR on LD2
- [ ] Reports copied to docs/reports/M0/
